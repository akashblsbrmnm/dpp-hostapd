You’re absolutely right. I don’t have visibility into the exact ambiorix API patterns, amxc_var initialization, callback wiring or event subscription style used in this codebase. Any code I write risks being wrong.

Here’s the revised plan — WHERE only, no code:

REVISED PLAN — LOCATION ONLY

PWHM CHANGES (pwhm repo — not prplMesh)

Phase A — ODL

pwhm/odl/40_wld-defaults-eps.odl
  → add DPP object under endpoint
  → add URI, Status parameters
  → add pushButton() function
  → mirror structure of existing WPS object


Phase B — Endpoint layer

pwhm/include/wld/wld_endpoint.h
  → declare wld_endpoint_dpp_start()

pwhm/include/wld/wld.h
  → add dpp_session_t struct into T_EndPoint
  → add mfn_wendpoint_dpp_start to T_CWLD_FUNC_TABLE
    (look at how mfn_wendpoint_wps_start is declared — mirror it exactly)

pwhm/src/wld_endpoint.c
  → add ODL handler _dppPushButton()
    (look at how _pushButton() for WPS is written — mirror it)
  → add wld_endpoint_dpp_start()
    (mirror wld_endpoint_wps_start() structure)


Phase C — wpa_supplicant backend

pwhm/src/nl80211/wld_wpaSupp_ep_api.c
  → add wifiGen_ep_dppStart()
    (look at wifiGen_ep_wpsStart() — same wpa_ctrl_send pattern)
  → sends: DPP_BOOTSTRAP_GEN, DPP_GET_URI
  → stores bootstrap_id and URI in dpp_session
  → sets DPP.URI and DPP.Status in DM

pwhm/src/Plugin/wifiGen_ep.c or wifiGen.c
  → wire mfn_wendpoint_dpp_start = wifiGen_ep_dppStart
    (look at how mfn_wendpoint_wps_start is wired)


Phase D — Cleanup

pwhm/src/wld_endpoint.c
  → add wld_endpoint_dpp_cleanup()
  → call it from wherever wld_endpoint_wps_cleanup() is called


PRPLMESH CHANGES — ENROLLEE PATH

Phase E — BWL WHM STA backend

common/beerocks/bwl/whm/sta_wlan_hal_whm.h
  → add start_dpp_bootstrap() declaration
  → add DPP_URI_READY to event enum

common/beerocks/bwl/whm/sta_wlan_hal_whm.cpp
  → add start_dpp_bootstrap()
    calls pwhm DPP.pushButton() via ambiorix
    look at how start_wps_pbc() calls InitiateWPSPBC — mirror exactly

  → add DPP.URI subscription
    look at lines 190-192 where WPS pairing done is subscribed
    mirror the same subscription pattern for DPP.URI change

  → in the subscription callback:
    push DPP_URI_READY event back to BackhaulManager
    look at how WPS_Pairing_Done event is pushed — mirror it

common/beerocks/bwl/sta_wlan_hal.h
  → add start_dpp_bootstrap() to pure virtual interface
  → add DPP_URI_READY to base Event enum

common/beerocks/bwl/dummy/sta_wlan_hal_dummy.cpp
  → add stub for start_dpp_bootstrap()


Phase F — BackhaulManager

agent/src/beerocks/slave/backhaul_manager/backhaul_manager.h
  → add start_dpp_bootstrap() declaration
  → add handle_dpp_uri_ready() declaration
  → add m_dpp_pbc_mode flag
  → add m_pending_dpp_uri storage

agent/src/beerocks/slave/backhaul_manager/backhaul_manager.cpp
  → in start_wps_pbc_sta() [line ~350]:
    add dpp_mode branch calling sta_wlan_hal->start_dpp_bootstrap()
    look at the existing sta_wlan_hal->start_wps_pbc() call — put dpp branch above it

  → add handle_dpp_uri_ready(uri):
    stores URI
    creates new internal message cACTION_BACKHAUL_DPP_URI_NOTIFICATION
    sends via m_agent_fd (same socket used at line ~3089 for WPS)

  → in FSM event polling where WPS_Pairing_Done is handled:
    add DPP_URI_READY case calling handle_dpp_uri_ready()
    look at how WPS events from sta_wlan_hal are consumed — mirror it

agent/src/beerocks/slave/nbapi/on_action.cpp
  → extend initiate_wps_pbc to accept optional dpp=true argument
  OR add InitiateDPPPBC alongside existing InitiateWPSPBC at line 42
  (your call — extending is simpler, separate is cleaner)


New internal message

common/beerocks/tlvf/yaml/beerocks_message_backhaul.yaml
  → add cACTION_BACKHAUL_DPP_URI_NOTIFICATION
    with uri string and enrollee_mac
    look at cACTION_BACKHAUL_START_WPS_PBC_REQUEST as template
  → regenerate autogenerated files


PRPLMESH CHANGES — PROXY AGENT PATH

Phase G — AP HAL WHM

common/beerocks/bwl/ap_wlan_hal.h
  → add WPS_DPP_URI to Event enum
  → add sWpsDppUriParams struct with uri + sta_mac

common/beerocks/bwl/whm/ap_wlan_hal_whm.cpp
  → in the raw pwhm event processing function:
    add parsing for "WPS-DPP-URI" string
    look at how "WPS_EVENT_TIMEOUT" is parsed — same string match pattern
    push WPS_DPP_URI event to queue


Phase H — ap_manager

agent/src/beerocks/fronthaul_manager/ap_manager/ap_manager.cpp
  → add WPS_DPP_URI case in event switch near line 3636
    (alongside WPS_Event_Timeout, WPS_Event_Fail, WPS_Event_Cancel)
    build cACTION_APMANAGER_DPP_URI_NOTIFICATION
    send via m_slave_fd

common/beerocks/tlvf/yaml/beerocks_message_apmanager.yaml
  → add cACTION_APMANAGER_DPP_URI_NOTIFICATION
    look at cACTION_APMANAGER_START_WPS_PBC_REQUEST as template


Phase I — son_slave_thread

agent/src/beerocks/slave/son_slave_thread.cpp
  → add case ACTION_APMANAGER_DPP_URI_NOTIFICATION near line 1803
    (alongside ACTION_BACKHAUL_START_WPS_PBC_REQUEST)
    build 1905.1 DPP_BOOTSTRAPPING_URI_NOTIFICATION_MESSAGE
    send via send_cmdu_to_broker

  → add case ACTION_BACKHAUL_DPP_URI_NOTIFICATION
    build same 1905.1 message
    send via send_cmdu_to_broker


Phase J — 1905.1 TLV

FIRST: grep -r "DPP_BOOTSTRAPPING_URI_NOTIFICATION" framework/tlvf/
  → if exists: use it, skip this phase
  → if not:
    framework/tlvf/yaml/wfa_map/
      → add tlvDppBootstrappingUriNotification.yaml
        with uri_str and enrollee_mac
        look at existing wfa_map TLV yamls as template
    → regenerate tlvf


CONTROLLER CHANGES

Phase K

FIRST: grep -r "dpp_protocol\|DppProtocol\|dpp_task\|DppTask" controller/src/
  → if dpp_protocol_task exists: add case there
  → if not: add in main controller cmdu handler

controller/src/beerocks/master/[dpp_task or main handler]
  → handle DPP_BOOTSTRAPPING_URI_NOTIFICATION_MESSAGE
    extract uri + enrollee_mac from TLV
    generate alias from enrollee_mac
    call g_database->parse_dpp_bootstrap_info()  ← YOUR EXISTING CODE
    create TR-181 ProvisionedDPP instance        ← YOUR EXISTING ODL
    send DPP CCE Indication                      ← proxy_agent_dpp_task handles rest


IMPLEMENTATION ORDER

1. pwhm Phase A → B → C → D   (foundation, no prplMesh impact)
2. Verify pwhm DPP.URI event fires correctly on device
3. Phase E (BWL sta_wlan_hal_whm)
4. Phase F (BackhaulManager)
5. Test: button press → URI in BackhaulManager logs
6. Phase J (check/add TLV)
7. Phase I (son_slave_thread routing)
8. Phase G + H (proxy agent AP side)
9. Phase K (controller)
10. End-to-end test


Key rule for all code writing: Before writing any ambiorix call, find the nearest existing function that does something similar in the same file and mirror its exact pattern. Never assume API shape.
